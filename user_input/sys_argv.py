import sys

x = int(sys.argv[1])  # Get the first command line argument
y = int(sys.argv[2])  # Get the second command line argument

z = x + y  # Add the two arguments
print(f"The result is: {z}")  # Print the result